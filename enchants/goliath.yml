display-name: "劣势爆发"
description: "当目标生命值超过你时，对其造成&a%damage%x &r的额外伤害，最多&a%limit%x"
placeholders:
  damage: "0.5 + %level% * 0.1"
  limit: "1.6 + %level% * 0.4"
type: normal

targets:
  - sword
conflicts: [ ]
rarity: rare
max-level: 6

tradeable: true
discoverable: true
enchantable: true

effects:
  - id: damage_multiplier
    args:
      multiplier: "1 + min(max(%victim_health% / %player_health% - 1, 0) * (0.5 + %level% * 0.1), 1.6 + %level% * 0.4)"
    triggers:
      - melee_attack

conditions: [ ]